/*
Arrays can hold more than one value. Can mix and match datatypes within the same array.

*/
var colors = ['blue', 'yellow', 'green', 'red'];

/*
Built-in Methods
Push - the push() method adds a value to the end of an array.
Pop - the pop() method removes the value at the end of an array and returns it.

*/
var numbers = [4,7,5,123,4,66,45]

console.log(numbers.length);

for (var i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}
